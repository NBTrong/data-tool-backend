export declare type InputFileFilter = {
  name?: string,
  url?: string,
  tab?: string,
  status?: string,
  row_count?: number,
  size?: number,
  limit?: number,
  page?: number,
  orderBy?: string,
  sortBy?: string,
  search?: string,
  from?: string,
  to?: string,
};

export declare type ExportInputFileFilter = {
  page?: number,
  limit?: number,
  orderBy?: string,
  sortBy?: string,
  search?: string,
  from?: string,
  to?: string,
};
